# LocalMind — Локальный ИИ ассистент для работы с базами знаний

**LocalMind** — это автономный инструмент на основе искусственного интеллекта, позволяющий загружать локальные документы (PDF, DOCX, TXT, Markdown), индексировать их и задавать вопросы на естественном языке. Весь процесс происходит **оффлайн**, без передачи данных в облако.

Я сделал эту сборку, для тех пользователей, кому сложно настраивать инструмент самому и тех, кого устраивает "все по умолчанию". Вам не понадобится устанавливать Python отдельно, все необходимы файлы уже приложены. Увы совсем отказаться от ручных шагов не получилось.

---

## 📦 Установка

### Требования
- ОС: Windows
- [Ollama](https://ollama.com/) — для запуска языковой модели (LLM)
- Минимум 4 ГБ оперативной памяти (рекомендуется 8 ГБ+ для больших моделей)

### Шаги

1. **Скачайте проект** (или клонируйте репозиторий):
   ```bash
   git clone https://github.com/RuslanKoynov/LocalMind.git
   cd localmind
   ```

2. **Установите Ollama**:  
   Следуйте инструкциям на [https://ollama.com/](https://ollama.com/)

3. **Загрузите языковую модель в Ollama** (выберите одну):

   Для лучшей поддержки русского языка:
   ```bash
   ollama pull qwen2.5:1.5b
   ```

   Либо более лёгкий вариант (работает и на слабых ПК):
   ```bash
   ollama pull phi3:mini
   ```

   > 💡 Модель `qwen2.5:1.5b` рекомендуется для русскоязычных документов.

---

## 🚀 Использование

### 1. Запуск сервера

Запустите LocalMindBox.exe

Сервер будет доступен по адресу:  
👉 [http://127.0.0.1:8000](http://127.0.0.1:8000)

### 2. Загрузка документов

- Поддерживаются форматы: `.txt`, `.md`, `.pdf`, `.docx`
- Можно загружать **один или несколько файлов одновременно**
- После загрузки файлы индексируются автоматически (может занять несколько секунд)

### 3. Задание вопросов

- Введите вопрос в поле «Задать вопрос»
- ИИ проанализирует ваши документы и даст ответ **с указанием источников**
- Если ответа в документах нет — система сообщит об этом

### 4. Примеры вопросов

- «Какие требования к установке системы?»
- «Кто автор документа "Руководство пользователя"?»
- «Перечисли шаги настройки модуля X»
- «Кто такая Татьяна Ларина?» (если есть файл с «Евгением Онегиным»)

### 5. Настройка "температуры ИИ"

Чем выше значение, тем больше "фантазии" от ИИ, вы получили в ответах, для текстового поиска по документам достаточно 0.3

---

## 🔐 Приватность и безопасность

- Все вычисления происходят **локально**
- Данные не передаются в интернет (после первоначальной загрузки модели)
- Векторная база хранится на вашем диске
- Можно использовать в корпоративной сети без риска утечки

---

## 🛠 Устранение неполадок

| Проблема | Решение |
|--------|--------|
| «Ошибка генерации» | Убедитесь, что Ollama запущен и модель загружена (`ollama list`) |
| Не загружаются PDF | Убедитесь, что файл не сканированный (требуется текстовый PDF) |
| Долгая загрузка | При первом запуске модель эмбеддингов скачивается (требуется интернет) |
| Сервер не запускается | Пишите мне, будем смотреть 😎 |


---

> 💡 **Совет**: для работы с большими коллекциями документов рекомендуется использовать SSD и как минимум 8 ГБ ОЗУ.

---

**LocalMind — ваш персональный ИИ-ассистент для знаний, полностью под вашим контролем.**
